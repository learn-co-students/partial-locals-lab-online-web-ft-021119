<%= form_tag students_path do %>
  <label>Search students:</label><br>
  <%= text_field_tag :query %><br>
  <%= submit_tag "Search" %>
<% end %>

<% @students.each do |one_student| %>
  <%= render partial: 'student', locals: {student: one_student} %>
<% end %>
